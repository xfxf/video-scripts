# This shell script is sourced during startup for supervisor

# Default commandline args (script local: does not need to be exported
SUPERVISOR=

# Make sure that other env vars are exported
export SCRIPTSROOT=$(realpath ~/lca/video-scripts)
export PYTHON=/usr/bin/python

